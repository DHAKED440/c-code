  #include<stdio.h>
  int gcd(int a, int b){
    if(b==0){
        return a;
    }
    return gcd(b,a%b);
  }
  int main () {
    int a,b;
    scanf("%d %d", &a, &b);
    int x=gcd(a,b);
    int LCM=(a*b)/x;
    printf("LCM of %d and %d is %d",a,b,LCM);
return 0;
  
  }
