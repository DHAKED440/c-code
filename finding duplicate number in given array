#include<stdio.h>
int main () {
    int size;
    printf("ENter the size ; ");
    scanf("%d", &size);
    int arr[size];
    printf("el of arr : ");
     for(int i=0; i<size; i++ ){
        scanf("%d", &arr[i]);
       }

       int found=0;
       for(int i=0; i<size; i++){
        for(int j=i+1; j<size; j++){
            if(arr[i]==arr[j]){
                printf("%d is aduplicate number",arr[i]);
                found=1;
                break;
            } 
        }
       }
       if(!found){
       printf("no duplicate number is present");
       }
       return 0;
}
